import React from "react";
import AdminCard from "@/components/admin/AdminCard";

export default function SettingsPage() {
    return (
        <div>
            <AdminCard title="Store Settings" description="Configure store settings and preferences">
                <p className="text-gray-600">
                    Settings page - Coming soon with store info, WhatsApp number, COD settings, and thresholds
                </p>
            </AdminCard>
        </div>
    );
}
